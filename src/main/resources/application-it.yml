#========================================
# Test profile settings (IT tests)
#========================================
logging:
  level:
    root: DEBUG
accessloader:
  access-level: R
scheduler:
  accessloader:
    cron: '-'
    municipalities:
      '2281':
        namespaces:
          MY_NAMESPACE:
            org-ids:
              - 1